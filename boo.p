(* comment *)
program main(input,output);
 var a,b,c: integer;
 var x,y,z: real;
 var p: array[3 .. 11] of integer;
 var q: array[13 .. 17] of real;
begin
 if ( not (b < c) ) then
   c := 0
 else
   c := 1
end.
